# Nile v0.16.8

- Fix desktop `Add connection` crashes caused by renderer-side interactive login mode lookups for Codex, Claude, and Gemini session flows.
- Move login interaction metadata onto renderer-safe builtin declarations so desktop surfaces no longer depend on runtime login registry registration.
- Keep browser-based Codex sign-in and terminal-based Gemini sign-in as explicit per-agent capabilities instead of implicit shared behavior.
