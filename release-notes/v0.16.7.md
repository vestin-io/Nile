# Nile v0.16.7

- Restore browser-based `Sign in with OpenAI` for Codex-scoped add-connection flows.
- Keep Codex sign-in isolated in a temporary `CODEX_HOME` so adding a connection does not mutate the current local Codex account.
- Remove the incorrect Terminal-wrapper login flow that printed long shell commands instead of letting Codex open the OpenAI web sign-in page itself.
