# Nile v0.16.5

- Fix desktop release CI for Cursor current-session import auto-binding.
- Build the desktop Cursor usage probe from the active desktop agent homes instead of process-global defaults.
- Remove the desktop Cursor auto-bind test's implicit dependency on host keychain state.
