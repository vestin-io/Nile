# CLI Surface

The CLI provides fast local workflows over the shared core.

It currently supports:

- listing saved connections
- creating connections
- importing current agent state
- switching the active connection
- reading current status
- listing mutation history

The CLI should stay presenter-driven and session-oriented.
