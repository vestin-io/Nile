# Desktop Surface

The desktop surface provides the same shared workflows through Electron.

It currently supports:

- menubar status and quick switching
- settings-driven connection management
- importing current state
- removing saved connections

The desktop surface should stay connection-first in product language and reuse the same core session flows as the CLI.
