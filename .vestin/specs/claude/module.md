# Claude Module

The Claude module implements the Claude agent adapter.

## Responsibilities

- read current Claude live state
- apply a saved connection into Claude local runtime state
- import current Claude state into a saved connection

Claude-specific settings, files, and auth layout stay inside this module.
